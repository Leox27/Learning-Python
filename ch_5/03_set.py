empty_set = set() # it creates empty set

# empty_set = {} # This is not craete an empty set
# An d values in set never be repeated if you do it never consider but code have no effect on
s =  {1, 2, 3, 6, 5, 9, 7, 8}

s.sort()
print(s)